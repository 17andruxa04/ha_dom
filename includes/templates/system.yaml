- trigger:
    - platform: time_pattern
      seconds: "/30"
  sensor:
    - name: "Current Time"
      state: "{{ now().strftime('%H:%M') }}"
      icon: mdi:clock

    - name: "Current Date"
      state: "{{ now().strftime('%d.%m.%Y') }}"
      icon: mdi:calendar
